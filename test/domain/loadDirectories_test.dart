/*import 'package:fex/infrastructure/ifile_system.dart';
import 'package:flutter_test/flutter_test.dart';

void main()
{
  test('Dado um dispositivo de arquivos \n'
      'Quando solicitar diretórios a partir da raiz \n'
      'Então deve retornar uma lista válida', () async
  {
    final fs = FileSystemFactory.fromCurrentPlatform();
    var dirs = fs.directoriesFromPath('/');
    final listDirs = await dirs.toList();
    expect(listDirs, isNotEmpty);
  } );
}

*/